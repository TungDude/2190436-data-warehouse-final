pyspark==3.5.4
pytest==8.3.4

# Silver -> gold tests use a real Postgres in Docker via testcontainers for
# SCD2 / SCD1 / fact upsert verification. boto3 + psycopg are imported by
# src/glue_jobs/silver_to_gold/common.py and are needed at test-collect time
# even when the testcontainers tests don't run.
boto3>=1.34
psycopg[binary]>=3.2,<3.3
testcontainers[postgres]>=4.0,<5.0
